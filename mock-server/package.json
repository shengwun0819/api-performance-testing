{
  "name": "k6-load-test-mock-server",
  "version": "1.0.0",
  "description": "Mock API server for k6 load testing (GET, POST, PATCH, PUT, DELETE)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  }
}